# Dev/test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
# ^ httpx.AsyncClient is the standard way to test a FastAPI app's HTTP
# routes without a running server. WebSocket routes are tested with
# FastAPI's own TestClient (starlette), no extra dependency needed.
